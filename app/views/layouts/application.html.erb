<!DOCTYPE html>
<html>
<head>
  <title>Tute Connect</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<nav class="navbar navbar-default">
		<div class="container">
			<div class="navbar-header">
				<%= link_to "Tute Connect", root_path, class: "navbar-brand" %>
			</div>
			<ul class="navbar-nav nav">
				<% if user_signed_in? %>
					<li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>

					<% Category.all.each do |category| %> 
					<li><%=link_to category.name, messages_path(category: category.name)%></li>
					<% end %>
				
				<% else %>	
					<li><%= link_to "Log In", new_user_session_path %></li>
					<li><%= link_to "Sign Up", new_user_registration_path %></li>
				<% end %>
			</ul>
			<% if user_signed_in? %>
				<p><%= link_to "New", new_message_path, class: "navbar-text navbar-link navbar-right" %></p>
			<% end %>
		</div>
	</nav>

	<p class="notice"><%= notice %></p>
	<p class="alert"><%= alert %></p>

	<div class="container animated fadeIn">
		<%= yield %>
	</div>

</body>
</html>
